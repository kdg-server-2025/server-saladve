1. db-deployment.yaml ファイルを作成
2. db-service.yaml ファイルを作成
3. secret.yaml ファイルを作成
4. db-pvc.yaml ファイルを作成
5. wp-deployment.yaml ファイルを作成
6. wp-servise.yaml ファイルを作成
7. wp-pvc.yaml ファイルを作成
8. kustomization.yaml ファイルを作成
9. compose.yaml ファイルを作成
10. `aqua g -i` を使用して kustomize を入れる
11. `aqua i -l` で追加したツールを使用できるようにする
12. `kustomize build . |kubectl apply -f -` を実行
13. seacret.yaml の StringDataのSをsに修正
13. `kind create cluster --config cluster.yaml` を実行
14. `kubectl port-forward service/wordpress 8080:8080` を実行
