# server-saladve

## １回目の授業

```bash
# SSH Key を作る（出力）コマンド
ssh-keygen -t ed25519

#ターミナル上にリポジトリURLを出力するコマンド
cat ~/.ssh/id_ed25519.pub

# git clone コマンド
git clone git@githubcom:kdg-server-2025/server-saladve.git

# cd コマンド
cd server-saladve
```